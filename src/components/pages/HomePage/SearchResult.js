import React from 'react'

function SearchResult() {
    const drinks = ["Result 1", "Cookie", "Call of duty: modern ass"]
   // const resultList = results.map(result => <a>{result}</a>)
    return (
        <div>
          
        </div>
    )
}

export default SearchResult
